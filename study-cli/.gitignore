node_modules/
.env.local
.env
.claude/settings.local.json
# Ignore anything a user drops into inbox/ or processed/, but keep the
# folder skeleton so git clone still has the directory layout.
inbox/*/*
!inbox/*/.gitkeep
processed/*
!processed/.gitkeep
*.log
.DS_Store
